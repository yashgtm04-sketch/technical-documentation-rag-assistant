"""
src package for the Technical Documentation Assistant.

This file's existence is what makes `src` importable as a package (e.g.
`from src.pipeline import answer_question`). It is intentionally non-empty:
a completely empty file has, in practice, been silently dropped by some
GitHub upload flows (particularly drag-and-drop web uploads), which causes
`ModuleNotFoundError: No module named 'src'` on deploy even though
everything looks correct locally. Keep this file tracked in git with real
content, not zero bytes.
"""
